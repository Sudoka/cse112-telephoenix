%h2= @phone.brand + ' ' + @phone.name
%p= link_to "Back", phones_path

%ul#tags
  - @tags.each do |tag|
    %li= tag.key + ": " + tag.value
    
  - @ratings.each do |rating|
    %li= "Rating: " + rating.rating.to_s

%table#reviews
  %col{:width => "200px"}/
  %thead
    %tr
      %th Pro Reviews
      %th User Reviews
      %th= link_to "New Review", new_phone_review_path(@phone)
    %tbody
      - @reviews.each do |review|
        %tr
          %td pro review here
          %td~ ("Rating: " + review.rating.to_s + "\nUser: " + review.user.username.to_s + "\n" + review.review_text)
          %td= link_to "View Comments (" + review.comments.count.to_s + ")", phone_review_comments_path(@phone, review)
